# 파이썬 캐시
__pycache__/
*.pyc

# 오프라인 설치 키트의 whl 파일
#   플랫폼별 바이너리(cp313-win_amd64)라 다른 환경에서는 쓸 수 없다.
#   필요하면 python scripts/make_offline_kit.py 로 각자 만든다.
dist/offline_kit/wheels/

# 루트의 DB는 커밋하지 않는다.
#   README 2단계(python scripts/init_db.py)가 실제로 DB를 만들어야
#   학생이 "표 5개를 만들었습니다 / 관광지 30곳..." 출력을 보게 된다.
#   반면 snapshots/ 안의 tour.db 는 커밋한다 —
#   진도가 뒤처진 학생이 폴더를 복사하자마자 바로 실행할 수 있어야 하기 때문.
/data/tour.db
