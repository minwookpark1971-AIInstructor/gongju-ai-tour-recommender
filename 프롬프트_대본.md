# Claude Code 프롬프트 대본 — 학생 배포용

> AI 관광 추천 서비스 (충청남도 공주시)
> 이 종이에 적힌 문장을 **순서대로** Claude Code에 붙여 넣으면 서비스가 완성된다.
> 그대로 복사만 하지 말고, 나온 코드를 읽고 **왜 그렇게 되는지** 생각해 볼 것.

---

## 시작 전에

작업할 폴더를 하나 만들고, 선생님이 준 `data` 폴더를 그 안에 넣는다.

```
tour-ai/
└── data/
    ├── spots_raw.csv      ← 관광지 원본 (32행)
    ├── users_raw.csv      ← 가상 이용자 50명
    └── ratings_raw.csv    ← 평점 300건
```

설치는 이 한 줄이 전부다.

```bash
pip install flask pandas
```

---

## Step 0 — 서버 띄우기

```
tour-ai 폴더에 Flask 웹서버 기본 파일 app.py를 만들어줘.
접속하면 "AI 관광 추천 서비스"라는 글자만 나오면 돼.
초보자가 이해할 수 있게 코드 줄마다 한글 주석을 달아줘.
```

**확인**: `python app.py` → 브라우저에서 `http://127.0.0.1:5000`

---

## Step 1 — 데이터 점검

```
data/spots_raw.csv를 읽어서 아래를 표로 출력하는 파이썬 코드를 만들어줘.
(1) 전체 행 수 (2) 컬럼별 결측치 개수 (3) 관광지명 중복 건수
(4) themes 컬럼에 등장하는 값의 종류와 각 개수
파일은 encoding='utf-8-sig'로 읽어줘. 한글이 깨지지 않게 하기 위해서야.
```

**확인해서 점검표에 적을 것**

| 항목 | 내가 찾은 값 |
|---|---|
| 전체 행 수 | |
| 소요시간이 비어 있는 행 | |
| 이름이 겹치는 관광지 | |
| 테마 이름 중 이상한 것 | |

> 힌트: `역사`와 `역사유적`은 같은 뜻인데 다르게 적혀 있다. 이런 걸 찾아야 한다.

---

## Step 2 — 정제하고 DB에 넣기

```
data/spots_raw.csv를 읽어서 아래 규칙으로 정제한 뒤 SQLite(data/tour.db)의
spots 테이블에 저장하는 scripts/preprocess.py와 scripts/init_db.py를 만들어줘.
규칙: (1) 관광지명 중복 제거
     (2) 소요시간 결측은 60분으로 채움
     (3) themes는 콤마로 구분된 1~3개이고, 각 항목이
         역사/자연/체험/맛집/사진스팟/휴식 6개 중 하나가 되도록 표기를 통일
         (예: 자연경관 → 자연, 먹거리 → 맛집)
정제 전/후 건수를 print로 비교해서 보여주고,
init_db.py는 인자 없이 실행하면 기존 DB를 건드리지 않고,
--reset 을 붙였을 때만 지우고 다시 만들게 해줘.
줄마다 한글 주석을 달아줘.
```

이어서 이용자와 평점도 넣는다.

```
init_db.py에 users, preferences, ratings, courses 표도 만들어줘.
data/users_raw.csv에는 닉네임·연령대·성별·동행유형과 테마 6개의 점수가 들어 있고,
data/ratings_raw.csv에는 닉네임·관광지명·점수가 들어 있어.
관광지명과 닉네임을 각각 id로 바꿔서 저장해줘.
```

**확인**: 정제 전 **32행** → 정제 후 **30행**이 나와야 한다.

---

## Step 3 — 데이터 분석 화면

```
/stats 라우트를 만들어줘. tour.db에서 아래 3가지를 조회해서 표로 보여줘.
(1) 테마별 관광지 수 (2) 연령대별 선호 테마 TOP3 (3) 평균 평점 상위 10곳
막대그래프는 차트 라이브러리 없이 div의 width를 퍼센트로 주는 방식으로 그려줘.
```

**확인**: 테마별 관광지 수 합계가 30보다 크다. 왜 그럴까? (관광지 하나가 테마를 여러 개 가질 수 있어서)

---

## Step 4 — 콘텐츠 기반 점수 ★가장 중요한 단계

```
recommender.py에 content_score(user_pref, spot_themes) 함수를 만들어줘.
테마 6개(역사/자연/체험/맛집/사진스팟/휴식) 순서를 고정한 벡터로 바꾼 뒤
코사인 유사도를 계산해서 0~1 값을 돌려줘.
넘파이 없이 순수 파이썬으로, 내적·크기·나눗셈 과정을 print로 볼 수 있게 해줘.
```

**반드시 손으로 계산해 볼 것**

```
내 선호     = [역사5, 자연3, 체험0, 맛집4, 사진스팟2, 휴식0]
공산성      = [    1,     0,     0,     0,         1,     0]   (테마: 역사, 사진스팟)

내적   = 5×1 + 3×0 + 0×0 + 4×0 + 2×1 + 0×0 = 7
크기A  = √(25+9+0+16+4+0) = √54 = 7.3485
크기B  = √(1+0+0+0+1+0)   = √2  = 1.4142
점수   = 7 ÷ (7.3485 × 1.4142) = 0.6736
```

컴퓨터가 낸 값이 **0.6736**과 같은지 확인한다. 다르면 코드가 틀린 것이다.

---

## Step 5 — 협업 필터링과 하이브리드

```
collab_score(user, spot_id, conn) 함수를 추가해줘.
아래 순서대로 시도해서 처음 데이터가 있는 단계의 평균 평점을 5로 나눠 0~1로 만들어줘.
1) 같은 연령대 + 같은 동행유형 사용자들의 평균
2) 없으면 같은 연령대 사용자들의 평균
3) 그래도 없으면 전체 사용자 평균
4) 평점이 하나도 없으면 0.5
그리고 몇 번째 단계를 썼는지 설명 문구도 같이 돌려줘. (점수, 문구) 형태로.
final_score = w*content + (1-w)*collab 로 합치는 함수도 만들어줘. w 기본값은 0.6.
```

> **왜 4단계나 두는가?**
> 새로 생긴 관광지는 평점이 없다. 1단계만 고집하면 대부분의 관광지가 0.5로 깔려서
> 추천이 밋밋해진다. 이걸 **콜드 스타트(cold start) 문제**라고 부른다.

---

## Step 6 — 코스 만들기

```
recommender.py에 make_course 함수를 추가해줘.
1) 모든 관광지를 final_score 내림차순으로 정렬
2) 여행시간이 3/6/9시간이면 목표 개수는 각각 3/4/5곳
3) 시간 예산은 여행시간 × 60분이고, 관광지 소요시간의 합만 계산한다
   (이동시간은 예산에 넣지 않는다)
4) 담았을 때 예산을 넘으면 그 관광지는 건너뛰고 다음 후보를 본다
5) 같은 테마는 코스에 최대 2곳까지만
6) 후보가 없어 3곳을 못 채우면 시간 제약을 풀고 3곳까지 채운다
7) 화면에 보여줄 총 소요시간은 소요시간 합 + 20분 × (곳수-1) 로 따로 계산한다
넘파이 없이 순수 파이썬으로, 왜 그 관광지를 건너뛰었는지 print로 보이게 해줘.
```

**확인**: 3시간 → 3곳, 6시간 → 4곳, 9시간 → 5곳이 나와야 한다.

---

## Step 7 — 화면 완성

```
templates 폴더에 base.html을 만들고 index/result/spots/spot_detail을 만들어줘.
- index.html: 닉네임, 연령대, 성별, 동행, 테마 6개 각각 0~5점 선택, 여행시간(3/6/9) 폼
- result.html: 코스 카드에 순번·관광지명·소요시간·비용·점수·추천이유를 보여주고
  맨 아래에 가중치 range 입력과 [다시 추천] 버튼을 둔다.
  이전 입력값은 hidden으로 다시 실어 보낸다. JavaScript는 쓰지 마.
- 사용자가 [추천받기]를 누르면 users와 preferences에 저장하고
  session["user_id"]에 넣어줘. secret_key도 설정해줘.
CSS는 static/style.css 한 파일에만 쓰고 색은 3개 이내로 해줘.
```

추천 이유 문장을 만들 때 주의할 점이 하나 있다.

```
추천 이유 문장을 만드는 함수를 만들어줘.
주의: 관광지가 가진 테마 중에서 '내가 가장 높은 점수를 준 테마'를 골라야 해.
관광지의 첫 번째 테마를 쓰면 안 돼.
예를 들어 베이커리밤마을의 테마는 '맛집,사진스팟'인데,
맛집에 2점 사진스팟에 5점을 준 사람에게 "선호하신 '맛집' 테마"라고 하면 틀린 설명이 돼.
```

---

## Step 8 — 오류 고치기 (수업 내내 반복)

```
아래 에러가 났어. 원인을 초보자도 알 수 있게 설명하고 고친 코드를 알려줘.
[에러 메시지 붙여넣기]
```

**자주 나오는 오류**

| 증상 | 원인 | 해결 |
|---|---|---|
| 한글이 `ìŠ¤` 처럼 깨진다 | CSV를 Excel로 열어 저장했다 | 원본을 다시 받는다. `utf-8-sig`로 읽는다 |
| `Address already in use` | 5000번 포트를 이미 쓰고 있다 | `app.run(port=5050)` 으로 바꾼다 |
| `no such table: spots` | DB를 아직 안 만들었다 | `python scripts/init_db.py` |
| `BuildError: 'spots'` | 템플릿이 아직 없는 라우트를 부른다 | 그 링크를 지우거나 라우트를 만든다 |
| 평점이 저장되지 않는다 | 첫 화면에서 취향 입력을 안 했다 | `/` 에서 추천을 한 번 받는다 |

---

## 제출물 (평가 15점 항목)

1. 내가 넣은 프롬프트와 나온 결과 **3건**
2. 오류가 났을 때 어떻게 물어봐서 고쳤는지 **기록 3건**
3. 이 서비스를 어떻게 개선하면 좋을지 **아이디어 1가지**
