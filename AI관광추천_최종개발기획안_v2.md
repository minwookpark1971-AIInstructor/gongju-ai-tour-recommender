# AI기반 맞춤형 관광 추천 서비스 — 최종 개발기획안 (v2.4)

> **대상**: 고등학교 경영정보 전공 학생 (개발 입문자)
> **원칙**: 내부 DB(SQLite) + 로컬호스팅 + 단일 폴더 — 설치·배포 없이 노트북 한 대에서 완주
> **확정 환경**: Python 3.13 (Windows) · Flask · SQLite
> **대상 지역**: **충청남도 공주시** (관광지 30곳 확정, §부록 A)
> **작성일**: 2026-08-30 · v1.0의 미결·모순 6건 + 시드 확정 후 4건을 반영 (변경 이력 §15)

---

## 1. 프로젝트 한 줄 정의

내 취향(연령·성별·동행·선호 테마·여행 시간)을 입력하면, 지역 관광지 DB에서
**나에게 맞는 3~5곳짜리 여행 코스**를 추천해 주는 웹 서비스.

이 프로젝트가 학생에게 남겨야 하는 것은 "돌아가는 웹사이트"가 아니라
**"추천 점수 하나를 자기 손으로 계산해 설명할 수 있는 상태"** 다. 모든 설계 결정이 이 목표에 종속된다.

---

## 2. 기술 스택 (의도적으로 최소화)

| 구분 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.10+ (검증 환경 3.13) | 학생들이 배운 언어 |
| 웹 프레임워크 | Flask | 파일 1개로 서버가 뜸 |
| 화면 | HTML + CSS + Jinja2 | React·빌드 도구 없음 |
| **JavaScript** | **사용하지 않음** | 폼 제출만으로 전 기능 구현 (§6.3) |
| DB | SQLite (내부 파일 DB) | 설치 불필요, `data/tour.db` 파일 하나 |
| 데이터 전처리 | pandas (표준 `csv` 폴백 병행) | pandas 설치 실패해도 수업이 안 멈춤 (§14) |
| 그래프 | CSS 막대 (`div` 너비 %) | 차트 라이브러리 없음 |
| 실행 | `python app.py` → `http://127.0.0.1:5000` | 로컬호스팅 |
| AI 협업 | Claude Code | 프롬프트로 코드 생성·수정 |

**설치는 이 한 줄이 전부**

```bash
pip install flask pandas
```

> 클라우드(Vercel/Supabase), 로그인/회원가입, 지도 API, JavaScript 프레임워크는 **범위 밖**.
> 필요하면 심화 과제로 뺀다(§13).

---

## 3. 폴더 구조

```
tour-ai/
├── app.py                  # 서버 + 라우팅 6개 (메인)
├── recommender.py          # 추천 알고리즘 (핵심 학습 파일)
├── db.py                   # DB 연결·조회 함수
├── requirements.txt
├── data/
│   ├── tour.db             # SQLite 내부 DB
│   ├── spots_raw.csv       # 관광지 원본 (32행, 일부러 지저분함)
│   ├── spots_clean.csv     # 정제 결과 (30행, preprocess.py가 생성)
│   ├── _expected_spots.csv # 정제 정답 (교사용, 학생 배포본에서 제외)
│   ├── users_raw.csv       # 가상 이용자 50명 + 선호 테마
│   └── ratings_raw.csv     # 평점 300건
├── scripts/
│   ├── make_seed.py        # 0단계: 시드 CSV 생성 (교사 전용)
│   ├── preprocess.py       # 1단계: 결측치·중복·표기 정제
│   ├── init_db.py          # 2단계: 정제 데이터 → tour.db 적재
│   └── verify.py           # 검증: 수업 전날 전항목 자동 점검 (교사 전용)
├── templates/
│   ├── base.html
│   ├── index.html          # 취향 입력 화면
│   ├── result.html         # 추천 코스 결과 + 가중치 조절
│   ├── spots.html          # 관광지 목록
│   ├── spot_detail.html    # 상세 + 평점 남기기
│   └── stats.html          # 데이터 분석 대시보드
└── static/
    ├── style.css
    └── images/
```

`make_seed.py`와 `verify.py`는 **교사만 실행**한다. 학생 배포본에서는 빼도 무방하다.

---

## 4. 데이터 설계

### 4.1 테이블 5개 (확정 스키마)

```sql
CREATE TABLE spots (
  id           INTEGER PRIMARY KEY,
  name         TEXT    NOT NULL,   -- 관광지명
  region       TEXT    NOT NULL,   -- 시·군·구
  themes       TEXT    NOT NULL,   -- 콤마 구분 1~3개. 예: '역사,체험'
  duration_min INTEGER NOT NULL,   -- 30 / 45 / 60 / 90 / 120 중 하나
  cost         INTEGER NOT NULL,   -- 1인 예상 비용(원), 0 = 무료
  indoor       INTEGER NOT NULL,   -- 1=실내 0=실외
  description  TEXT,               -- 한 줄 소개
  image        TEXT                -- static/images 파일명 (없으면 NULL)
);

CREATE TABLE users (
  id         INTEGER PRIMARY KEY,
  nickname   TEXT,
  age_group  TEXT,   -- '10대' | '20대' | '30대' | '40대이상'
  gender     TEXT,   -- '남' | '여' | '응답안함'
  companion  TEXT,   -- '혼자' | '친구' | '가족'
  created_at TEXT
);

CREATE TABLE preferences (
  id      INTEGER PRIMARY KEY,
  user_id INTEGER,
  theme   TEXT,      -- 표준 6개 중 하나
  weight  INTEGER    -- 0~5
);

CREATE TABLE ratings (
  id         INTEGER PRIMARY KEY,
  user_id    INTEGER,
  spot_id    INTEGER,
  score      INTEGER,   -- 1~5
  created_at TEXT
);

CREATE TABLE courses (
  id          INTEGER PRIMARY KEY,
  user_id     INTEGER,
  spot_ids    TEXT,      -- 콤마 구분, 방문 순서대로
  total_score REAL,
  created_at  TEXT
);
```

### 4.2 테마 표준값 6개 — 순서 고정

```
역사(0) · 자연(1) · 체험(2) · 맛집(3) · 사진스팟(4) · 휴식(5)
```

이 **순서가 곧 벡터의 인덱스**다. 코드 어디서든 이 순서를 바꾸면 안 된다.
6개로 고정해야 벡터 길이가 6으로 고정되고, 학생이 손으로 계산해 볼 수 있다.

**관광지 하나에 테마는 1~3개까지 허용한다.**
(v1.0에서 "하나로만 통일"과 "콤마 구분"이 충돌했음 — 테마가 하나뿐이면 코사인 유사도가 사실상 무의미해지므로 다중값으로 확정)

### 4.3 시드 데이터 규모와 **의도적 편향**

- 관광지 **30곳** — **충청남도 공주시로 확정**. 네이버 지역검색으로 실재 확인한 장소만 사용 (목록 §부록 A, 원본 파일 `data/spots_raw.csv`)
- 가상 사용자 **50명** — 4연령 × 3동행 = 12조합이 **모두 1명 이상** 존재해야 함
- 평점 **300건**

**관광지 30곳 확정 지표** (실측)

| 항목 | 값 |
|---|---|
| 소요시간 분포 | 30분 5곳 · 45분 10곳 · 60분 8곳 · 90분 4곳 · 120분 3곳 → **평균 정확히 60.0분** |
| 테마 분포 | 역사 11 · 자연 11 · 사진스팟 11 · 휴식 11 · 체험 8 · 맛집 6 (총 58개 태그) |
| 실내/실외 | 14곳 / 16곳 (확장과제 '날씨 반영'이 동작할 균형) |
| 무료/유료 | 15곳 / 15곳 (확장과제 '예산 상한'이 동작할 균형) |
| 지역(읍면동) | 12개 |

맛집이 6곳으로 가장 적지만 문제되지 않는다 — "같은 테마 최대 2곳" 규칙 때문에
한 코스에 맛집이 3곳 이상 들어갈 수 없어, 6곳이면 어떤 조합이든 충분하다.

**평점 300건은 무작위로 만들면 안 된다.** 누가 입력하든 추천 결과가 비슷해져서
수업 데모가 실패한다. 아래 편향을 반드시 심는다.

| 축 | 편향 |
|---|---|
| 10대 | 체험 · 사진스팟에 4~5점 |
| 20~30대 | 맛집 · 사진스팟에 4~5점 |
| 40대이상 | 역사 · 휴식에 4~5점 |
| 가족 동행 | 체험 · 자연 가산 |
| 혼자 | 휴식 · 사진스팟 가산 |

**편향이 실제로 심겼는지 확인** (`make_seed.py`가 출력하는 실측 평균 평점)

```
10대     : 사진스팟 3.71 · 맛집 3.62 · 체험 3.44
20대     : 맛집 4.14 · 사진스팟 3.67 · 체험 3.27
30대     : 체험 3.65 · 맛집 3.57 · 자연 3.16
40대이상 : 휴식 3.82 · 역사 3.77 · 자연 3.35
```

> 확인은 반드시 **평균 평점**으로 해야 한다. 합계로 보면 역사 태그를 가진 관광지가
> 11곳, 맛집은 6곳이라 태그 개수에 끌려가 편향이 있는지 없는지 판별할 수 없다.

또한 **평점이 하나도 없는 관광지를 2곳 남겨 둔다**(대통길작은미술관, 자연미술관Ko).
신규 등록 관광지 상황을 만들어 §5.2 폴백 4순위가 실제로 발동하게 하기 위함이다.

### 4.4 원본 CSV는 일부러 지저분하게 만든다 ★

`data/spots_raw.csv`는 **깨끗하면 안 된다.** Step 1(데이터 점검)과 Step 2(정제)가
이 수업 1단계의 전부인데, 깔끔한 CSV를 주면 그 3개 차시가 통째로 사라진다.

정제 기준 30행에 아래 오염을 주입해 **원본 32행**을 만든다.

| 오염 유형 | 건수 | 내용 |
|---|---|---|
| 중복 행 | 2 | 공산성(이름 뒤 공백 `"공산성 "`), 마곡사(설명만 다름) |
| `duration_min` 결측 | 3 | 신원사, 고마나루솔밭, 대통길작은미술관 → 정제 시 60분으로 채움 |
| 테마 표기 흔들림 | 19행 | `역사유적`→역사 · `자연경관`→자연 · `먹거리`→맛집 · `포토존`→사진스팟 · `힐링`→휴식 · `체험학습`→체험 |

**정제 정답: 32행 → 중복 2건 제거 → 30행, 결측 3건 보정, 표기 19건 통일.**
이 숫자가 §12 루브릭 '데이터 정제 20점'의 채점 기준이 된다.
교사용 정답 파일은 `data/_expected_spots.csv`로 따로 보관한다(학생 배포본에서 제외).

---

## 5. 추천 알고리즘 (`recommender.py`)

### 5.0 함수 시그니처 (확정)

```python
THEMES = ["역사", "자연", "체험", "맛집", "사진스팟", "휴식"]   # 순서 고정

def to_pref_vector(pref_dict) -> list          # {'역사':5,...} → [5,3,0,4,2,0]
def to_spot_vector(themes_text) -> list        # '역사,사진스팟' → [1,0,0,0,1,0]
def cosine(a, b) -> float                      # 순수 파이썬 (numpy 없음)
def content_score(user_pref, spot_themes) -> float           # 0~1
def collab_score(user, spot_id, conn) -> (float, str)        # (점수, 근거문구)
def final_score(content, collab, w_content=0.6) -> float
def make_course(user, spots, conn, hours, w_content) -> list
def build_reason(spot, user_pref, c, cf_note) -> str         # 추천 이유 문장
```

`collab_score`가 점수와 **근거 문구를 함께 반환**하는 것이 핵심 설계다.
§5.2의 폴백 단계를 화면에 그대로 노출하기 위함이다.

### 5.1 콘텐츠 기반 점수 — `content_score`

1. 사용자 선호를 6차원 벡터로 변환 → `[역사5, 자연3, 체험0, 맛집4, 사진2, 휴식0]`
2. 관광지 테마도 6차원 0/1 벡터로 변환 → `역사,사진스팟` → `[1,0,0,0,1,0]`
3. **코사인 유사도** 계산 → 0~1 점수

**손계산 검증 예제 (5-6차시에 학생이 직접 푼다)**

```
사용자 A = [5, 3, 0, 4, 2, 0]
관광지 '○○읍성' = [1, 0, 0, 0, 1, 0]        (테마: 역사, 사진스팟)

내적       = 5×1 + 3×0 + 0×0 + 4×0 + 2×1 + 0×0 = 7
|A|        = √(25+9+0+16+4+0) = √54  = 7.3485
|읍성|     = √(1+0+0+0+1+0)   = √2   = 1.4142
content    = 7 ÷ (7.3485 × 1.4142) = 7 ÷ 10.3923 = 0.6736
```

`verify.py`는 이 값이 코드 출력과 **소수점 4자리까지 일치**하는지 자동 확인한다.

### 5.2 협업 필터링 점수 — `collab_score` (폴백 4단계)

"나와 비슷한 사람 = 같은 연령대 + 같은 동행유형"이라는 규칙 기반 단순화를 유지한다.
다만 그 조건만 쓰면 시드 50명 기준 조합당 4명뿐이라 대부분의 관광지가 0.5(중립)로 깔려
**추천이 밋밋해진다.** 그래서 폴백 체인을 둔다.

```
1순위: 같은 연령대 + 같은 동행유형의 평균 평점
2순위: (1순위 데이터 0건) 같은 연령대의 평균 평점
3순위: (2순위도 0건)      전체 사용자 평균 평점
4순위: (평점 자체가 0건)  0.5 (중립)

점수 = 평균평점 ÷ 5   → 0~1로 환산
```

이 폴백은 우회가 아니라 **"데이터가 부족할 때 어떻게 하는가(cold start)"를 가르치는 장치**다.
몇 순위가 쓰였는지 근거 문구로 반환해 결과 화면에 그대로 노출한다.

- 1순위 → `"또래·가족 이용자 평점 4.3점"`
- 3순위 → `"평점 데이터가 적어 전체 평균 3.8점을 사용"`

### 5.3 최종 점수 (하이브리드)

```
final = w_content × content_score + (1 - w_content) × collab_score
기본값 w_content = 0.6
```

앞의 예제로 이어가면:

```
content = 0.6736, collab = 4.3 ÷ 5 = 0.86
final   = 0.6 × 0.6736 + 0.4 × 0.86 = 0.4042 + 0.3440 = 0.7482
```

가중치는 결과 화면에서 슬라이더로 조절 가능하게 만든다(§6.3).
학생이 값을 바꾸며 결과 변화를 관찰하는 것이 알고리즘 체감의 핵심이다.

### 5.4 코스 생성 규칙 — `make_course` ★확정

v1.0은 "시간 초과 시 중단"과 "최소 3곳"을 동시에 요구해 규칙이 서로를 부쉈다.
(3시간=180분을 골랐는데 관광지 평균이 90분이면 2곳에서 끊겨 최소 3곳을 못 채움)
**시간을 중단 조건이 아니라 목표 개수로 환산**하여 해소한다.

```
① 목표 개수    : 3시간 → 3곳 / 6시간 → 4곳 / 9시간 → 5곳
② 시간 예산    : 선택한 시간 × 60분   ← 관광 시간만. 이동시간은 예산에 넣지 않는다
③ 담는 순서    : final_score 내림차순

담기 루프:
   후보를 위에서부터 본다
   → 같은 테마가 이미 코스에 2곳 있으면 건너뛴다
   → 담았을 때 Σ(duration_min)이 시간 예산을 넘으면 건너뛰고 다음 후보로
   → 목표 개수를 채우면 종료

예외 처리:
   후보가 바닥났는데 3곳 미만이면 → 시간 제약을 풀고 3곳까지 채운다
   이때 결과 화면에 "예상보다 N분 초과" 배지를 표시한다

표시용 총 소요시간 (예산 계산에는 쓰지 않음):
   총 소요 = Σ(duration_min) + 20분 × (곳수 - 1)
   결과 화면 문구 예: "관광 2시간 45분 + 이동 40분 = 총 3시간 25분"
```

**이동시간을 예산에서 뺀 이유** — 시드 30곳(§부록 A)으로 3000회 시뮬레이션한 결과다.

| 방식 | 3시간 폴백률 | 6시간 목표달성 |
|---|---|---|
| 이동 30분을 예산에 포함 | **64%** | 92% |
| 이동 15분을 예산에 포함 | 35% | 99% |
| **이동을 예산에서 제외 (채택)** | **15%** | **100%** |

이동시간을 예산에 넣으면 3시간 코스의 3분의 2에서 폴백이 터진다. 예외 배지가 상시로 뜨면
배지 자체가 의미를 잃는다. 사용자가 "3시간"을 고르는 것도 실제로는 **관광에 쓸 시간**을 뜻하므로,
예산은 관광 시간만 재고 이동시간은 정직하게 따로 보여주는 쪽이 맞다.

각 관광지마다 **추천 이유 문장**을 자동 생성한다.

```
"선호하신 '역사' 테마와 67% 일치, 또래·가족 이용자 평점 4.3점"
```

건너뛴 이유는 `print`로 콘솔에 남긴다 — 학생이 알고리즘의 판단 과정을 눈으로 보게 하기 위함이다.

---

## 6. 화면 설계

### 6.1 라우팅 6개

| 메서드 | 경로 | 화면 | 핵심 요소 |
|---|---|---|---|
| GET | `/` | 취향 입력 | 닉네임, 연령대, 성별, 동행, 테마 6개 각 0~5점, 여행시간(3/6/9), [추천받기] |
| POST | `/recommend` | 추천 결과 | 코스 카드 3~5장(순번·소요시간·비용 합계), 카드마다 점수+추천이유, 가중치 슬라이더 |
| GET | `/spots` | 관광지 목록 | 테마 필터(`?theme=`), 카드 그리드 |
| GET | `/spots/<id>` | 상세 | 소개, 평균 평점, 별점 남기기 폼 |
| POST | `/spots/<id>/rate` | (처리) | ratings INSERT → 상세로 리다이렉트 |
| GET | `/stats` | 대시보드 | 테마별 관광지 수, 연령대별 선호 테마 TOP3, 평점 상위 10곳 |

`/recommend` 처리 순서: **users·preferences INSERT → session 저장 → 코스 생성 → courses INSERT → 렌더**

### 6.2 사용자 식별 — Flask session (회원가입 없음)

v1.0에는 사용자를 식별하는 방법이 없어 평점 저장이 불가능했다.

- `/`에서 [추천받기]를 누르는 순간 `users`에 INSERT하고 `session["user_id"]`에 저장
- 세션 없이 상세 페이지에 오면 평점 폼 대신 **"먼저 첫 화면에서 취향을 입력해 주세요"** 안내
- `app.secret_key`는 하드코딩하되 *"실제 서비스라면 비밀로 관리한다"* 주석을 반드시 단다

### 6.3 가중치 슬라이더 — JavaScript 0줄

`result.html` 안에 폼을 하나 더 둔다.

```html
<form method="post" action="/recommend">
  <!-- 이전 입력값을 전부 hidden 으로 다시 싣는다 -->
  <input type="hidden" name="age_group" value="{{ user.age_group }}">
  ...
  <input type="range" name="w_content" min="0" max="1" step="0.1" value="{{ w_content }}">
  <button>이 가중치로 다시 추천</button>
</form>
```

제출하면 `/recommend`가 다시 돌면서 화면이 새로 그려진다. **JS가 한 줄도 필요 없다.**

### 6.4 디자인 규칙

카드형 UI · 1컬럼 반응형 · 색상 3개 이내 · CSS는 `style.css` 한 파일.
`/stats`의 막대그래프는 `div`의 `width: N%`로 그린다.
이미지가 없는 관광지는 **테마 색 배경 + 관광지명 텍스트**로 대체된다(저작권 회피 겸용).

---

## 7. 개발 단계 (Step 0~8) — 교육 4단계 매핑

| Step | 작업 | 산출물 | 교육 단계 |
|---|---|---|---|
| 0 | 폴더 생성, Flask 설치, "Hello" 페이지 | 서버 실행 성공 | 준비 |
| 1 | `spots_raw.csv` 열어 결측치·중복·표기흔들림 확인 | 데이터 점검표 | 1단계 |
| 2 | `preprocess.py` 정제 → `init_db.py` 적재 | `tour.db` | 1단계 |
| 3 | `/stats` 화면으로 데이터 분포 확인 | 대시보드 | 1단계 |
| 4 | `content_score()` 구현 + **손계산 검증** | 콘텐츠 추천 동작 | 2단계 |
| 5 | `collab_score()` + 하이브리드 결합 | 최종 점수 | 2단계 |
| 6 | `make_course()` 코스 생성 규칙 | 코스 3~5곳 | 2단계 |
| 7 | 입력·결과·목록·상세 화면 완성 | 웹 서비스 완성 | 3·4단계 |
| 8 | 오류 수정, 가중치 튜닝, 발표 리허설 | 시연 + 발표자료 | 4단계 |

> **3단계(생성형 AI 활용)는 별도 차시가 아니라 Step 1~8 전체에 녹아 있다.**
> 모든 단계에서 학생이 Claude Code에 프롬프트를 넣고 → 실행 → 오류 → 다시 질문하는 루프를 돈다.

### 7.1 수업 차시 배분 (50분 × 12차시)

| 차시 | Step | 내용 |
|---|---|---|
| 1 | 0 | 환경 설치, Hello 서버 |
| 2 | 1 | CSV 점검표 작성 |
| 3 | 2 | 정제 + DB 적재, 전/후 건수 비교 |
| 4 | 3 | `/stats`로 분포 확인 |
| **5-6** | **4** | **content_score + 손계산 검증 (핵심 차시)** |
| 7 | 5 | collab_score + 하이브리드 |
| 8 | 6 | make_course |
| 9-10 | 7 | 화면 5개 완성 |
| 11 | 8 | 가중치 튜닝, 오류 수정 루프 |
| 12 | 8 | 발표·시연 |

5-6차시가 이 수업의 심장이다. 학생이 코사인 유사도를 자기 손으로 한 번 계산해 봐야
§12 루브릭의 '추천 알고리즘 이해' 25점이 살아난다. **이 차시만은 절대 줄이지 않는다.**

---

## 8. 사전 제작 작업 순서 (교사 트랙)

이 프로젝트는 두 트랙으로 굴러간다.

| 트랙 | 주체 | 산출물 | 시점 |
|---|---|---|---|
| **A. 사전 제작** | 교사 | 레퍼런스 완성본, 시드 CSV, Step별 스냅샷, 배포 키트 | 수업 전 |
| **B. 수업 재현** | 학생 | Claude Code 프롬프트로 A를 스스로 다시 만듦 | 수업 중 |

**A를 100% 끝내야 B가 안전하다.** 학생이 막혔을 때 붙여넣을 정답이 없으면 수업이 멈춘다.

| # | 작업 | 완료 기준 | 예상 |
|---|---|---|---|
| A1 | 폴더 뼈대 + `requirements.txt` + Hello 서버 | 127.0.0.1:5000 응답 | 0.5h |
| A2 | `make_seed.py` → 관광지 30곳 CSV | 6테마 고른 분포, duration 5구간 | 2h |
| A3 | `make_seed.py` → 사용자 50·평점 300 CSV | §4.3 편향 5종 반영 | 2h |
| A4 | `preprocess.py` | 정제 전/후 건수 로그 출력 | 1h |
| A5 | `init_db.py` (5테이블, `--reset` 분기) | `tour.db` 생성, 재실행 안전 | 1.5h |
| A6 | `db.py` 조회 함수 | 전 라우트가 쓸 함수 완비 | 1h |
| A7 | `content_score` | 손계산 0.6736과 일치 | 2h |
| A8 | `collab_score` (폴백 4단계) | 4경로 모두 도달 확인 | 2h |
| A9 | `make_course` (§5.4 규칙) | 3/6/9시간 → 3/4/5곳 | 2.5h |
| A10 | `verify.py` | §10 체크리스트 전항목 PASS | 1.5h |
| A11 | 템플릿 6종 + `style.css` | 5화면 전부 렌더 | 4h |
| A12 | 슬라이더 재추천 폼 | 0.9/0.1로 순서 변동 확인 | 1h |
| A13 | Step별 스냅샷 (`step2_done`~`step7_done`) | 각 폴더 단독 실행됨 | 1.5h |
| A14 | 프롬프트 대본 최종본 검증 | 순서대로 넣어 완주 1회 | 2h |
| A15 | 배포 키트 (오프라인 whl + 실행 안내) | 인터넷 끊고 설치 성공 | 1h |
| | **합계** | | **약 25.5h (3~4일)** |

**임계 경로: A2 → A3 → A5 → A8 → A9 → A10**
시드 데이터(A2·A3)가 부실하면 A8·A9의 결과가 밋밋해지고 수업 데모 자체가 실패한다.
**A2·A3에 시간을 아끼지 않는다.**

---

## 9. Claude Code 프롬프트 대본 (학생 배포용)

학생은 아래 프롬프트를 **순서대로 복사·입력**한다. 전문은 교재 부록으로 제본.

**Step 0 — 서버 띄우기**

```
tour-ai 폴더에 Flask 웹서버 기본 파일 app.py를 만들어줘.
접속하면 "AI 관광 추천 서비스"라는 글자만 나오면 돼.
초보자가 이해할 수 있게 코드 줄마다 한글 주석을 달아줘.
```

**Step 1 — 데이터 점검**

```
data/spots_raw.csv를 읽어서 아래를 표로 출력하는 파이썬 코드를 만들어줘.
(1) 전체 행 수 (2) 컬럼별 결측치 개수 (3) 관광지명 중복 건수
(4) themes 컬럼에 등장하는 값의 종류와 각 개수
파일은 encoding='utf-8-sig'로 읽어줘. 한글이 깨지지 않게 하기 위해서야.
```

**Step 2 — 정제 + DB 적재** *(v1.0 대본 수정본)*

```
data/spots_raw.csv를 읽어서 아래 규칙으로 정제한 뒤 SQLite(data/tour.db)의
spots 테이블에 저장하는 scripts/preprocess.py와 scripts/init_db.py를 만들어줘.
규칙: (1) 관광지명 중복 제거
     (2) 소요시간 결측은 60분으로 채움
     (3) themes는 콤마로 구분된 1~3개이고, 각 항목이
         역사/자연/체험/맛집/사진스팟/휴식 6개 중 하나가 되도록 표기를 통일
         (예: 자연경관 → 자연, 먹거리 → 맛집)
정제 전/후 건수를 print로 비교해서 보여주고,
init_db.py는 인자 없이 실행하면 기존 DB를 건드리지 않고,
--reset 을 붙였을 때만 지우고 다시 만들게 해줘.
줄마다 한글 주석을 달아줘.
```

**Step 3 — 대시보드**

```
/stats 라우트를 만들어줘. tour.db에서 아래 3가지를 조회해서 표로 보여줘.
(1) 테마별 관광지 수 (2) 연령대별 선호 테마 TOP3 (3) 평균 평점 상위 10곳
막대그래프는 차트 라이브러리 없이 div의 width를 퍼센트로 주는 방식으로 그려줘.
```

**Step 4 — 콘텐츠 기반 점수**

```
recommender.py에 content_score(user_pref, spot_themes) 함수를 만들어줘.
테마 6개(역사/자연/체험/맛집/사진스팟/휴식) 순서를 고정한 벡터로 바꾼 뒤
코사인 유사도를 계산해서 0~1 값을 돌려줘.
넘파이 없이 순수 파이썬으로, 내적·크기·나눗셈 과정을 print로 볼 수 있게 해줘.
```

**Step 5 — 협업 필터링 + 하이브리드**

```
collab_score(user, spot_id, conn) 함수를 추가해줘.
아래 순서대로 시도해서 처음 데이터가 있는 단계의 평균 평점을 5로 나눠 0~1로 만들어줘.
1) 같은 연령대 + 같은 동행유형 사용자들의 평균
2) 없으면 같은 연령대 사용자들의 평균
3) 그래도 없으면 전체 사용자 평균
4) 평점이 하나도 없으면 0.5
그리고 몇 번째 단계를 썼는지 설명 문구도 같이 돌려줘. (점수, 문구) 형태로.
final_score = w*content + (1-w)*collab 로 합치는 함수도 만들어줘. w 기본값은 0.6.
```

**Step 6 — 코스 생성** *(v1.0에 없던 대본, 신규)*

```
recommender.py에 make_course 함수를 추가해줘.
1) 모든 관광지를 final_score 내림차순으로 정렬
2) 여행시간이 3/6/9시간이면 목표 개수는 각각 3/4/5곳
3) 총 소요시간 = 관광지 소요시간 합 + 30분 × (곳수-1) 로 계산하고,
   시간 예산(시간×60분)을 넘으면 그 관광지는 건너뛰고 다음 후보를 본다
4) 같은 테마는 코스에 최대 2곳까지만
5) 후보가 없어 3곳을 못 채우면 시간 제약을 풀고 3곳까지 채운다
넘파이 없이 순수 파이썬으로, 왜 그 관광지를 건너뛰었는지 print로 보이게 해줘.
```

**Step 7 — 화면 완성**

```
templates 폴더에 base.html을 만들고 index/result/spots/spot_detail을 만들어줘.
- index.html: 닉네임, 연령대, 성별, 동행, 테마 6개 각각 0~5점 선택, 여행시간(3/6/9) 폼
- result.html: 코스 카드에 순번·관광지명·소요시간·비용·점수·추천이유를 보여주고
  맨 아래에 가중치 range 입력과 [다시 추천] 버튼을 둔다.
  이전 입력값은 hidden으로 다시 실어 보낸다. JavaScript는 쓰지 마.
- 사용자가 [추천받기]를 누르면 users와 preferences에 저장하고
  session["user_id"]에 넣어줘. secret_key도 설정해줘.
CSS는 static/style.css 한 파일에만 쓰고 색은 3개 이내로 해줘.
```

**Step 8 — 오류 수정 루프**

```
아래 에러가 났어. 원인을 초보자도 알 수 있게 설명하고 고친 코드를 알려줘.
[에러 메시지 붙여넣기]
```

---

## 10. 검증 체크리스트 (`verify.py` 자동 확인)

**수업 전날 이 스크립트 하나로 전부 통과해야 한다.**

| # | 항목 |
|---|---|
| 0 | `spots_raw.csv` **32행** → 정제 후 `spots` **30행** (중복 2건 제거, 결측 3건 보정) |
| 1 | `spots` 30행, 모든 테마가 표준 6개 안에만 존재 (`역사유적`·`먹거리` 등 잔존 0건) |
| 2 | `duration_min` 결측 0건, 값이 {30,45,60,90,120} 안에 존재, 평균 60.0분 |
| 3 | 관광지명 중복 0건 (앞뒤 공백 제거 후 비교) |
| 4 | `users` 50행, 12개(연령×동행) 조합이 **모두 1명 이상** |
| 5 | `ratings` 300행, score 전부 1~5 |
| 6 | `content_score`가 §5.1 손계산 예제(0.6736)와 소수점 4자리까지 일치 |
| 7 | `collab_score` 폴백 1·2·3·4순위가 각각 최소 1회 발동 |
| 8 | 3/6/9시간 요청 시 각각 3/4/5곳 반환 |
| 9 | 한 코스에 동일 테마 3곳 이상 없음 |
| 10 | **10대·혼자 vs 40대이상·가족 코스가 최소 2곳 이상 다름** ← 데모 성패 |
| 11 | 가중치 0.9 vs 0.1일 때 코스 순서가 바뀜 |
| 12 | `init_db.py` 인자 없이 재실행해도 기존 평점이 보존됨 |

10번이 실패하면 시드 편향(§4.3)이 부족한 것이다. **A3으로 돌아간다.**

---

## 11. 실행 방법 (학생용 3줄)

```bash
pip install flask pandas          # 최초 1회
python scripts/init_db.py         # DB 만들기 (최초 1회)
python app.py                     # 서버 켜기 → 브라우저에서 127.0.0.1:5000
```

DB를 처음부터 다시 만들려면 (경고: 남긴 평점이 사라짐)

```bash
python scripts/init_db.py --reset
```

발표 때 옆 조에게 보여주려면 `app.run(host="0.0.0.0", port=5000)`으로 바꾸면
같은 와이파이의 다른 노트북에서 `http://내IP:5000`으로 접속 가능하다.

---

## 12. 평가 루브릭 (100점)

| 항목 | 배점 | 확인 방법 |
|---|---|---|
| 데이터 정제 | 20 | 정제 전/후 건수 비교 설명 |
| 추천 알고리즘 이해 | 25 | **점수 하나를 손으로 계산해 설명** |
| 웹 화면 동작 | 25 | 입력→추천→상세→평점 전 과정 시연 |
| AI 협업 과정 | 15 | 프롬프트·오류 수정 기록 3건 제출 |
| 발표·기획 | 15 | 서비스 개선 아이디어 1가지 제안 |

---

## 13. 확장 과제 (빠른 조/심화반) — **5개 모두 구현 완료**

**설계 원칙: 기본 완성본을 복잡하게 만들지 않는다.**
확장 기능은 전부 *기본값이 꺼짐*인 선택 인자로 붙였다. 아무것도 주지 않으면
기본 과제와 완전히 똑같이 동작하며, 이는 `verify.py` 13항목이 회귀 테스트로 보증한다.

| # | 확장 | 구현 | 켜는 방법 |
|---|---|---|---|
| 1 | 날씨에 따라 실내 관광지 가중치 | `recommender.weather_bonus()` — 비 +0.15, 더움·추움 +0.10 | 첫 화면 '심화 설정'에서 날씨 선택 |
| 2 | 예산 상한으로 코스 비용 제한 | `make_course(max_cost=...)` — 넘으면 그 관광지를 건너뜀 | '심화 설정'에서 예산 선택 |
| 3 | 코스를 지도 위에 순번으로 표시 | `geo.to_svg_points()` + `result.html`의 인라인 SVG | 추천받으면 자동 표시 |
| 4 | 누적 데이터로 "인기 코스" 화면 | `/popular` 라우트 + `db.popular_spots()` / `popular_courses()` | 상단 메뉴 '인기 코스' |
| 5 | 이동시간을 실제 거리로 계산 | `geo.travel_minutes()` — 하버사인 거리 × 1.3 ÷ 40km/h | '심화 설정'에서 체크 |

### 13.1 좌표 데이터 (`data/spots_geo.csv`)

확장 3·5는 좌표가 필요하다. 30곳 전부 **네이버 지역검색 결과의 `mapx`/`mapy` 값**을
그대로 옮겼다(경도·위도 = 값 ÷ 10⁷). 임의로 지어낸 좌표는 하나도 없다.

기본 과제 데이터(`spots_raw.csv`)는 **손대지 않았다.** 좌표는 별도 파일로 두고
`init_db.py`가 그 파일이 있을 때만 `lat`/`lon` 컬럼을 채운다.
파일이 없어도 기본 기능은 그대로 동작한다.

거리 검산 (상식과 맞는지 확인용)

```
공산성 → 국립공주박물관   직선  1.3km → 10분
공산성 → 마곡사          직선 14.7km → 30분
마곡사 → 동학사          직선 29.4km → 55분
```

### 13.2 지도를 그리며 부딪힌 문제 두 가지

**(가) 코스가 한 점으로 뭉갠다.**
관광지 30곳 전체(약 30km)에 맞춰 축척을 잡으면, 코스가 전부 원도심(반경 1.5km)일 때
점 네 개가 겹쳐 무엇이 무엇인지 알 수 없다.
→ 축척을 **코스에 담긴 곳 기준**으로 잡고 1.6배 여유를 준다. 최소 범위는 위도 0.02도(약 2.2km).

**(나) 이름표가 서로 겹친다.**
원도심 관광지들은 서로 1km 안쪽이라 이름표를 어떻게 배치해도 충돌한다.
→ **지도에서 이름표를 뺐다.** 번호 동그라미만 찍고 이름은 바로 아래 코스 목록이 담당한다.
번호가 목록과 1:1로 대응하므로 정보 손실이 없고, 충돌 문제 자체가 사라진다.
겹치는 동그라미는 최소 34px까지 서로 밀어내되(`geo.separate()`),
"위치를 조금 벌려 놓았다"고 화면에 명시한다.

> 이 그림은 **지도 API를 쓰지 않는다.** 좌표를 SVG 점으로 찍은 것뿐이라
> 인터넷 연결도, 외부 서비스 가입도 필요 없다. 기획안 §2의 '지도 API 범위 밖' 원칙을 지킨다.

---

## 14. 리스크와 대비

| 리스크 | 심각도 | 대비 |
|---|---|---|
| **한글 CSV 인코딩 깨짐** (Excel로 열어 저장) | 높음 | 모든 CSV 읽기를 `encoding='utf-8-sig'`로 고정. "CSV를 Excel로 열어 저장하지 말 것" 명시 |
| 협업 필터링 결과가 밋밋함 | 높음 | 시드 평점 300건에 연령·동행별 편향을 심음(§4.3) + `verify.py` 10번 항목으로 자동 검증 |
| 학생별 진도 편차 | 높음 | Step별 완성본 폴더(`step3_done` 등) 사전 준비, 뒤처진 학생은 복사해서 이어감 |
| pip 설치 실패 (학교망 차단) | 중간 | 오프라인 설치용 whl 파일 USB 배포 |
| Python 3.13 + pandas 설치 실패 | 중간 | `preprocess.py`를 표준 `csv` 모듈로도 돌아가게 이중 작성 |
| DB 파일 손상 / 초기화 사고 | 중간 | `tour.db` 백업본 + `init_db.py`는 `--reset` 없이는 덮어쓰지 않음 |
| 포트 5000 충돌 | 낮음 | `port=5050` 등으로 변경 안내 |
| 관광지 이미지 30장 부재 | 낮음 | 이미지 없으면 테마 색 배경 + 관광지명 텍스트로 대체 (저작권 회피 겸용) |

가장 위험한 것은 **인코딩**이다. 학생 30명 중 몇 명은 반드시 CSV를 Excel로 열어 저장하고,
그 순간 한글이 깨진다. Step 1 프롬프트에 `utf-8-sig`를 못박아 둔 이유다.

---

## 15. v1.0 → v2.0 변경 이력

| # | v1.0 상태 | v2.0 확정 | 근거 |
|---|---|---|---|
| 1 | "시간 초과 시 중단" + "최소 3곳" 규칙 충돌 | 시간을 **목표 개수**로 환산 (3/6/9h → 3/4/5곳), 이동시간 30분 명시 | 3시간에 평균 90분짜리면 2곳에서 끊겨 최소 3곳 불가 |
| 2 | 테마 "콤마 구분"(§4.1) vs "하나로만 통일"(§8) 모순 | **1~3개 다중값** 확정 | 테마가 하나뿐이면 코사인 유사도가 무의미 |
| 3 | 이웃 = 연령+동행 단일 조건 | **폴백 4단계** (동행+연령 → 연령 → 전체 → 0.5) + 근거 문구 반환 | 12조합×4명으로는 대부분 0.5 중립 처리되어 추천이 밋밋해짐 |
| 4 | 사용자 식별 방법 없음 (평점 저장 불가) | **Flask session**, 회원가입 없음 | `/spots/<id>` 평점에 `user_id`가 필요 |
| 5 | "슬라이더로 조절" (구현 방법 미정) | hidden 필드 + range 재제출, **JS 0줄** | JS는 학생 수준 초과, 디버깅 비용 과다 |
| 6 | `init_db.py` 재실행 시 평점 전멸 | 인자 없으면 no-op, `--reset`일 때만 재적재 | Step 7~8에서 반드시 한 번은 발생하는 사고 |
| 7 | 시드 CSV 제작 주체 없음 ("교사가 배포") | `scripts/make_seed.py` 신규 + 편향 설계 명시 | CSV를 만드는 작업 자체가 임계 경로의 시작 |
| 8 | 검증 절차 없음 | `scripts/verify.py` + 12항목 체크리스트 | 수업 당일 실패를 전날 잡기 위함 |
| 9 | Step 6 프롬프트 대본 누락 | 신규 작성 (§9) | 코스 생성이 가장 규칙이 복잡한 단계 |
| 10 | 인코딩·pandas·이미지 리스크 미식별 | 리스크 3건 추가 (§14) | Windows 한글 환경에서 실제 발생 |

### v2.0 → v2.1 (시드 데이터 확정 후)

| # | 변경 | 근거 |
|---|---|---|
| 11 | 대상 지역 **공주시 확정**, 30곳 실명 목록 확정 (§부록 A) | 네이버 지역검색으로 실재 확인 |
| 12 | 이동시간을 **시간 예산에서 제외**, 20분은 표시용으로만 사용 (§5.4) | 실제 시드로 3000회 시뮬레이션 → 예산에 포함 시 3시간 코스 폴백률 64%로 배지가 무의미해짐 |
| 13 | **§4.4 원본 CSV 오염 설계** 신규 | 깨끗한 CSV를 주면 Step 1~2(3개 차시)가 통째로 사라짐 |
| 14 | 검증 체크리스트에 0번(32행→30행) 추가 | 정제 정답 수치를 루브릭 채점 기준으로 고정 |

### v2.1 → v2.2 (구현 중 발견)

| # | 변경 | 근거 |
|---|---|---|
| 15 | 테마 표기 흔들림 **9행 → 19행**으로 정정 | 실제 정제 실행 결과. 문서 수치가 틀렸음 |
| 16 | `build_reason`이 관광지의 **첫 번째 테마가 아니라, 내가 가장 높은 점수를 준 테마**를 고르도록 수정 | 베이커리밤마을(`맛집,사진스팟`)을 맛집 2점·사진스팟 5점인 학생에게 "선호하신 '맛집' 테마"로 설명하면 완전히 틀린 문장이 된다 |
| 17 | `verify.py` 4·5번을 **"정확히 50명/300건" → "시드 50명 유지 / 300건 이상"** 으로 변경 | 학생이 앱을 한 번만 써도 users·ratings가 늘어 검증이 헛되이 실패한다. 검사할 것은 총량이 아니라 시드 보존 여부 |
| 18 | `data/`에 `users_raw.csv` 추가 (v2.0은 `ratings_raw.csv` 하나만 상정) | 이용자 속성과 평점을 한 파일에 넣으면 정규화가 무너져 적재 코드가 복잡해진다 |

### v2.2 → v2.3 (A13~A15 완료)

| # | 변경 | 근거 |
|---|---|---|
| 19 | Step별 스냅샷을 **손으로 만들지 않고 생성기로 자동 생성** (`make_snapshots.py`) | 6개 폴더를 복사해 두면 완성본을 고칠 때마다 전부 옛 코드로 남는다. 완성본에서 잘라내면 절대 어긋나지 않는다 |
| 20 | 스냅샷 Step3~6 전용 `base.html`을 따로 생성 | 완성본 `base.html`의 내비게이션이 `url_for('spots')`를 부르는데 그 단계 앱에는 아직 `/spots` 라우트가 없어 `BuildError`가 난다 |
| 21 | 스냅샷 Step4~6에 `try_recommend.py` 추가 (기획안에 없던 파일) | 그 단계에는 아직 화면이 없어 학생이 자기 코드가 도는지 확인할 방법이 없다. 손계산 차시(5-6차시)의 실습 도구 |
| 22 | **오프라인 키트를 Flask 전용으로 축소 (23MB → 0.6MB)** | pandas를 넣으면 numpy까지 딸려 와 23MB. 이 프로젝트는 pandas를 CSV 읽기에만 쓰고 `csv` 폴백이 있어, pandas 없이 전 기능이 동작함을 빈 venv에서 확인 |
| 23 | `check_prompts.py` 신규 — 대본과 완성본의 정합성 자동 검사 | 대본의 파일명·함수 시그니처·숫자가 완성본과 어긋나면 학생은 대본대로 했는데 다른 결과를 본다 |

### v2.3 → v2.4 (확장과제 5개 구현)

| # | 변경 | 근거 |
|---|---|---|
| 24 | 확장 5개를 **기본값이 꺼진 선택 인자**로 구현 (`weather`, `max_cost`, `real_distance`) | 심화 기능 때문에 본 수업의 레퍼런스가 어려워지면 안 된다. 기본 동작 불변은 `verify.py` 13/13이 회귀 테스트로 보증 |
| 25 | 좌표를 `spots_geo.csv`로 **분리** (기본 `spots_raw.csv` 미변경) | 좌표 컬럼을 원본에 넣으면 Step1 '결측·중복 찾기' 실습에 잡음이 섞인다 |
| 26 | 지도 축척을 전체 30곳이 아니라 **코스 기준**으로 확대 | 코스가 전부 원도심이면 점 네 개가 한 덩어리로 뭉개져 읽을 수 없다 |
| 27 | 지도에서 **이름표 제거**, 번호 동그라미만 표시 | 원도심 관광지는 1km 안쪽이라 이름표가 어떻게 배치해도 충돌한다. 이름은 아래 코스 목록이 담당하므로 정보 손실이 없다 |
| 28 | `weather_bonus`를 recommender.py **5구역으로 이동** | 4구역에 두면 Step5 스냅샷에 딸려 들어가, 그 단계 대본에 없는 코드가 학생에게 보인다 |
| 29 | `base.html` 내비게이션에 '인기 코스' 추가 + Step3~6 스냅샷용 `base.html` 동기화 | 라우트가 없는 단계에서 `url_for` 오류가 나지 않도록 |

> **A14의 한계를 명시한다.** `check_prompts.py`가 검사하는 것은 *대본과 완성본이 서로 모순되지 않는가*
> 뿐이다. Claude Code가 그 프롬프트로 **실제로 어떤 코드를 만들어 낼지는 검사할 수 없다.**
> 빈 폴더에서 대본을 순서대로 넣어 완주해 보는 것은 사람이 한 번 직접 해야 한다.

---

## 16. 착수 상태 — 트랙 A 전체 완료

**대상 지역: 충청남도 공주시로 확정.** 레퍼런스 완성본이 동작하며 검증 13항목 전부 통과.

| # | 작업 | 상태 | 산출물 |
|---|---|---|---|
| A1 | 폴더 뼈대 + requirements | ✅ | `requirements.txt`, `.claude/launch.json` |
| A2 | 관광지 30곳 시드 | ✅ | `data/spots_raw.csv`(32행) · `_expected_spots.csv`(30행) |
| A3 | 이용자 50명 · 평점 300건 | ✅ | `scripts/make_seed.py` → `users_raw.csv` · `ratings_raw.csv` |
| A4 | 정제 | ✅ | `scripts/preprocess.py` (pandas + csv 이중 구현) |
| A5 | DB 적재 | ✅ | `scripts/init_db.py` (`--reset` 분기 동작 확인) |
| A6 | 조회 함수 | ✅ | `db.py` |
| A7 | content_score | ✅ | 손계산 0.6736 일치 |
| A8 | collab_score | ✅ | 폴백 4단계 전부 발동 확인 |
| A9 | make_course | ✅ | 3/6/9시간 → 3/4/5곳 |
| A10 | 검증 | ✅ | `scripts/verify.py` — **13/13 통과** |
| A11 | 템플릿 6종 + CSS | ✅ | 5화면 렌더 확인 |
| A12 | 가중치 재추천 폼 | ✅ | JavaScript 0줄 |
| A13 | Step별 스냅샷 | ✅ | `scripts/make_snapshots.py` → `snapshots/step2~7_done` (6폴더 단독 실행 확인) |
| A14 | 프롬프트 대본 | ✅ | `프롬프트_대본.md` + `scripts/check_prompts.py` — **12/12 일치** |
| A15 | 오프라인 배포 키트 | ✅ | `scripts/make_offline_kit.py` → `dist/offline_kit` (**0.6MB**, 빈 venv 설치 성공) |

**검증 통과 실측치** (`python scripts/verify.py`)

```
[PASS]  0. spots_raw.csv 32행 → 정제 후 30행
[PASS]  2. 소요시간 {30,45,60,90,120}, 평균 60.0분
[PASS]  6. content_score 손계산 0.6736 일치
[PASS]  7. 협업 필터링 폴백 1·2·3·4순위 모두 발동
[PASS]  8. 3/6/9시간 → 3/4/5곳
[PASS] 10. 10대·혼자 vs 40대이상·가족 → 서로 다른 곳 4곳
             10대·혼자   : 곡물집, 베이커리밤마을, 공주목재문화체험장, 계룡산상신농촌체험휴양마을
             40대이상·가족: 마곡사, 신원사, 대장이랜드중장농촌체험휴양마을, 공산성본가
결과: 13 / 13 통과
```

---

## 부록 A. 시드 관광지 30곳 (공주시)

전부 **네이버 지역검색으로 실재를 확인한 장소**다. 아래는 정제 후 기준값이며,
학생에게 배포되는 `spots_raw.csv`는 여기에 §4.4의 오염이 주입된 32행이다.

| # | 관광지 | 지역 | 테마 | 소요 | 비용 | 실내 |
|---|---|---|---|---|---|---|
| 1 | 공산성 | 금성동 | 역사·자연·사진스팟 | 90 | 3,000 | 실외 |
| 2 | 공주무령왕릉과왕릉원 | 웅진동 | 역사 | 45 | 3,000 | 실외 |
| 3 | 국립공주박물관 | 웅진동 | 역사·체험 | 60 | 무료 | 실내 |
| 4 | 석장리박물관 | 석장리동 | 역사·체험 | 60 | 1,300 | 실내 |
| 5 | 충청남도역사박물관 | 중동 | 역사 | 30 | 무료 | 실내 |
| 6 | 마곡사 | 사곡면 | 역사·자연·휴식 | 90 | 무료 | 실외 |
| 7 | 갑사 | 계룡면 | 역사·자연 | 90 | 무료 | 실외 |
| 8 | 동학사 | 반포면 | 역사·자연 | 90 | 무료 | 실외 |
| 9 | 신원사 | 계룡면 | 역사·휴식 | 45 | 무료 | 실외 |
| 10 | 천주교황새바위순교성지 | 금성동 | 역사·휴식 | 45 | 무료 | 실외 |
| 11 | 공주한옥마을 | 웅진동 | 역사·사진스팟 | 30 | 무료 | 실외 |
| 12 | 유구색동수국정원 | 유구읍 | 자연·사진스팟 | 45 | 무료 | 실외 |
| 13 | 계룡저수지 | 계룡면 | 자연·휴식 | 45 | 무료 | 실외 |
| 14 | 고마나루솔밭 | 웅진동 | 자연·휴식·사진스팟 | 60 | 무료 | 실외 |
| 15 | 공주산림휴양마을 | 금학동 | 자연·휴식 | 120 | 5,000 | 실외 |
| 16 | 공주산성시장문화공원 | 산성동 | 휴식·사진스팟 | 30 | 무료 | 실외 |
| 17 | 공주목재문화체험장 | 금학동 | 체험 | 60 | 5,000 | 실내 |
| 18 | 대장이랜드중장농촌체험휴양마을 | 계룡면 | 체험·자연 | 120 | 15,000 | 실외 |
| 19 | 계룡산상신농촌체험휴양마을 | 반포면 | 체험·자연 | 120 | 15,000 | 실외 |
| 20 | 아트센터고마 | 웅진동 | 체험·휴식 | 45 | 무료 | 실내 |
| 21 | 임립미술관 | 계룡면 | 사진스팟·휴식 | 45 | 5,000 | 실내 |
| 22 | 이미정갤러리 | 반죽동 | 사진스팟·휴식 | 30 | 무료 | 실내 |
| 23 | 대통길작은미술관 | 반죽동 | 사진스팟·체험 | 60 | 무료 | 실내 |
| 24 | 자연미술관Ko | 중동 | 사진스팟·자연 | 45 | 3,000 | 실내 |
| 25 | 공주산성시장 | 산성동 | 맛집·체험 | 60 | 10,000 | 실외 |
| 26 | 개성집공주점 | 금성동 | 맛집 | 45 | 12,000 | 실내 |
| 27 | 고마나루1999 | 금성동 | 맛집 | 60 | 15,000 | 실내 |
| 28 | 공산성본가 | 금성동 | 맛집 | 45 | 13,000 | 실내 |
| 29 | 베이커리밤마을 | 금성동 | 맛집·사진스팟 | 30 | 8,000 | 실내 |
| 30 | 곡물집 | 봉황동 | 맛집·사진스팟·휴식 | 45 | 9,000 | 실내 |

**선정 원칙**

1. **실재 확인** — 네이버 지역검색 결과에 주소가 `충청남도 공주시`로 찍힌 곳만 채택.
   (검색 결과에 섞여 나온 계룡시·논산시 소재 장소는 전부 제외)
2. **테마 균형** — 6개 테마가 각각 최소 6곳 이상 걸리도록 배분. 한 테마에만 몰리면
   그 테마를 고른 학생만 결과가 풍성해진다.
3. **소요시간 균형** — 평균 60분에 맞춰 §5.4의 3/6/9시간 규칙이 성립하도록 배분.
4. **실내/실외, 무료/유료 반반** — §13 확장과제(날씨 가중치, 예산 상한)가 실제로 동작하도록.
5. **비용은 "1인 예상 비용"** — 사찰 관람료 등은 변동이 잦으므로 정확한 요금표가 아니라
   교육용 추정치다. 수업에서 이 점을 명시한다.
